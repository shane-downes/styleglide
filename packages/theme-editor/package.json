{
  "name": "@styleglide/theme-editor",
  "license": "MIT",
  "version": "0.0.10",
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "unpkg": "dist/auto.min.js",
  "files": [
    "dist/",
    "README.md",
    "LICENSE"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsup",
    "lint": "eslint . --max-warnings 0",
    "push": "pnpm run build && yalc push",
    "release": "pnpm run build && changeset publish"
  },
  "devDependencies": {
    "@workspace/eslint-config": "workspace:^",
    "@workspace/typescript-config": "workspace:*",
    "tsup": "^6.5.0",
    "typescript": "^5.7.3"
  }
}
